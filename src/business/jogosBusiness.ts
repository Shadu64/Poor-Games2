//aqui é onde ficam as verificação

// aqui criamos uma classe, com funcoes dentro

class JogosBusiness{
    // cria um objeto da classe jogoData
    cadastrarJogo = async (nome:string, genero: string, dataLancamento:string) =>{
        // validaçoes, verificacoes, criacao de id, e chama um metodo para executar a query

        // jogoData.cadastrarJogo(nome, genero, datalancamento)
    }
}